coverage
**/dist/**/*.*

# Build output. `lib/` is emitted by tsc/tsup, `public/` by webpack.
**/lib/**
apps/commons/public/**

# Vendored third-party assets, kept byte-for-byte as shipped: TinyMCE's skins,
# the bundled jQuery/ObjTree copies, and the browserified jsdom build.
**/css/tinymce/**
packages/cwrc-leafwriter/src/lib/jquery/**
packages/cwrc-leafwriter/src/lib/objtree/**
packages/cwrc-leafwriter-validator/src/lib/jsdom/**

# Anything minified is not ours to reformat.
**/*.min.*

# Code-generated sources — reformatting them only drifts from what the
# generator emits (see visual-design:sync and the avatar/asset scripts).
**/generated/**
**/generated*.ts

# Bundled GlyphWiki KAGE core data (scripts/extract-glyphwiki-kage-core.mjs):
# a flat 229,000-entry name->data map kept single-line/compact on purpose -
# prettier's default object formatting bloats this ~5% for no benefit and
# drifts from what the extraction script emits on a regenerate.
packages/cwrc-leafwriter/src/resources/glyphwiki/kageCore.json

# Bundled GlyphWiki compound-glyph index (scripts/build-glyphwiki-compound-index.mjs):
# 282,518 compact [name, kageData, componentA, componentB] tuples, same
# single-line-on-purpose rationale as kageCore.json above.
packages/cwrc-leafwriter/src/resources/glyphwiki/glyphwikiCompoundIndex.json

# Bundled IDS-to-Unicode decomposition index (scripts/build-ids-unicode-index.mjs):
# 91,708 compact [ids, char] tuples, same single-line-on-purpose rationale.
packages/cwrc-leafwriter/src/resources/ids/idsUnicodeIndex.json

# Wide reference tables. Prettier pads every cell to its column's widest entry,
# and these hold whole UI strings in four languages: formatting the German
# triage table takes it from 89KB to 784KB, all of it trailing spaces, and makes
# every row ~1500 characters. The content is fine; the alignment pass is not.
docs/german-localization-triage.md
